07/10 
- if: 조건에 따른 흐름 분기/제어
 1. if( boolean ) code 실행문
 2. if( boolean ){ code; code; }
 3. if( boolean ){ code; }
     else{ code; }
 4. if( boolean ){
       code
    }else if(boolean){
    }else if(boolean){
    }else{     }
 5. if( boolean ) {  }
    if( boolean ) { }
    if( boolean ) { }
 6. if
      if( boolean ){
	if( boolean ) {
          }  
        } 
